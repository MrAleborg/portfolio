[pytest]
DJANGO_SETTINGS_MODULE = portfolio.settings
python_files = test_*.py
filterwarnings =
    # WhiteNoise looks for STATIC_ROOT, which only exists after collectstatic.
    ignore:No directory at.*staticfiles:UserWarning
